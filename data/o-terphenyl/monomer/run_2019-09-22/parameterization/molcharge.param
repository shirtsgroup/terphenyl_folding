#Interface settings

#molcharge :   
    -in  MON_new.pdb
    -method  am1bccsym
    -out  MON_new.mol2
    #-param (Not set, no default)
    #-paramfile (Not set, no default)
    #-prefix  molcharge
